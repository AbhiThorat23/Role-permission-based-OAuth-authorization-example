# Role-permission-based-OAuth-authorization-example
Role and permission based OAuth Authorization in microservices which has profile service as resource server and auth-service as Authorization server
